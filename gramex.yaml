# Generated by gramex init 1.57.0 (don't delete this line)
# on 2019-10-16 by Jaidev Deshpande <deshpande.jaidev@gmail.com>

# Configurations for app: modelhandler-demo
# ----------------------------------------------------------------------------
url:
  modelhandler-demo-home:
    pattern: /$YAMLURL/
    handler: FileHandler
    kwargs:
      path: $YAMLPATH/index.html
      # See https://learn.gramener.com/guide/auth/#authorization for auth rules
      template: true
  uploadhandler:
    pattern: /$YAMLURL/initform
    handler: FunctionHandler
    kwargs:
      function: modelhandler_demo.init_form
      xsrf_cookies: false
      redirect: /$YAMLURL/
  modelhandler:
    pattern: /$YAMLURL/fit
    handler: FunctionHandler
    kwargs:
      function: modelhandler_demo.fit
      xsrf_cookies: false
      headers:
        Content-Type: text/plain
        Cache-Control: no-store
  formhandler:
    pattern: /$YAMLURL/preview
    handler: FormHandler
    kwargs:
      url: $GRAMEXDATA/apps/mlhandler/data.csv
      xsrf_cookies: false
  chart:
    pattern: /$YAMLURL/chart.png
    handler: FileHandler
    kwargs:
      path: $YAMLPATH/chart.png
      headers:
        Cache-Control: no-store

# Gramex init configurations for app: modelhandler-demo
# ----------------------------------------------------------------------------
import:
  ui:
    path: $GRAMEXAPPS/ui/gramex.yaml
    YAMLURL: $YAMLURL/ui/

handlers:
  BaseHandler:
    # Custom error pages for this application
    error:
      400: {path: $YAMLPATH/error/400.html}     # Bad request: e.g. URL argument missing
      401: {path: $YAMLPATH/error/401.html}     # Unauthorized: User not logged in
      403: {path: $YAMLPATH/error/403.html}     # Forbidden: user does not have access
      404: {path: $YAMLPATH/error/404.html}     # File not found
      500: {path: $YAMLPATH/error/500.html}     # Internal server error
